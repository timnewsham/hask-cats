# Playing with category translations

Playing with some ideas from some of Conal's papers on compiling
with categories, and constructing stack machines.

Run the program:
`stack run hask-cats`

Use dot to visualize the graph: `./g`
